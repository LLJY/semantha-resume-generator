# Project family overlays

Use this directory to group multiple repositories into one product or platform family.

Example use cases:

- one backend repo, one mobile client repo, one admin repo, one frontend repo
- one firmware repo plus one backend/CV repo in the same product family
- one school capstone split across app, IoT, and docs repos

Family overlays help the ranker avoid selecting five near-duplicate repos from the same body of work.
